<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.octopusden.octopus.jira</groupId>
        <artifactId>jira-utils-parent</artifactId>
        <version>2.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>common</artifactId>
    <name>Octopus Jira Utils Library Commons</name>

    <properties>
        <!-- required by maven-crm-plugin 5.x -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    
        <!-- Report-only, and only because of where the findings are: all seven Checkstyle
             violations in this module are in the Java stubs gmavenplus generates for the Groovy
             tests, which octopus-parent 2.2.0 registers as a source root. octopus-parent#13 stops
             the analysers descending into generated sources - built against that branch this
             module reports 0. Flip this to true once 2.2.1 is released. -->
        <octopus.quality.failOnViolation>false</octopus.quality.failOnViolation>
        <!-- The coverage gate is independent of the above and is enforced: measured 0.6410. -->
        <octopus.coverage.failOnViolation>true</octopus.coverage.failOnViolation>
        <octopus.coverage.minimumLine>0.60</octopus.coverage.minimumLine>
</properties>
    <dependencies>
        <dependency>
            <groupId>org.octopusden.octopus.releng</groupId>
            <artifactId>releng-lib</artifactId>
        </dependency>
    </dependencies>
</project>
