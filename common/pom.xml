<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>org.octopusden.octopus.jira</groupId>
        <artifactId>jira-utils-parent</artifactId>
        <version>2.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>common</artifactId>
    <name>Octopus Jira Utils Library Commons</name>

    <properties>
        <!-- required by maven-crm-plugin 5.x -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    
        <!-- Strict, as everywhere else. The seven Checkstyle findings this module used to report were
             all in the Java stubs gmavenplus generates for the Groovy tests; octopus-parent 2.2.1 keeps
             the analysers out of generated sources, so the count is 0. -->
        <!-- Coverage is gated separately and enforced: measured 0.6410. -->
        <octopus.coverage.failOnViolation>true</octopus.coverage.failOnViolation>
        <octopus.coverage.minimumLine>0.60</octopus.coverage.minimumLine>
</properties>
    <dependencies>
        <dependency>
            <groupId>org.octopusden.octopus.releng</groupId>
            <artifactId>releng-lib</artifactId>
        </dependency>
    </dependencies>
</project>
